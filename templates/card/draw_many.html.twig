{% extends "base.html.twig" %}

{% block title %}Draw Cards{% endblock %}

{% block body %}
<div class="two-col-layout">
            <aside class="aside">
                <div class="">
                    <h2> Meny</h2>
                        <ul>
                            <li><a href="{{ path('card') }}">Landing page</li>
                            <li><a href="{{ path('deck') }}">Deck</li>
                            <li><a href="{{ path('shuffle') }}">Shuffles</li>
                            <li><a href="{{ path('draw') }}">Draw</a></li>
                            <li><a href="{{ path('reset') }}">Reset</a></li>
                            {# <li><a href="{{ path('draw_many') }}">Draw many</a></li> #}
                        </ul>
                </div>
            </aside>

    <main class="main">
        <h1>Draw more then one card</h1>

        {# <p>How many cards do you want to draw from the deck?</p>

        <form method="post" action="">
            <input type="number" name="num_cards" min="1" max="52" value="1">
            <input type="submit" name="do_it" value="Draw cards">
        </form> #}

        <ul>
            {% for card in cards %}
                {% if '♥' in card %}
                    <div class="red">
                        <li>{{card}}</li>
                    </div>

                {% elseif '♦️' in card %}
                    <div class="red">
                        <li>{{card}}</li>
                    </div>

                {% else %}
                    <div>
                        <li>{{card}}</li>
                    </div>
                {% endif %}
            {%endfor%}

                <li>Antal kort kvar {{card_left}}</li>
        </ul>
    </main>
</div>
{% endblock %}