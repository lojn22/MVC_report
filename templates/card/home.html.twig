{% extends "base.html.twig" %}

{% block title %}Card{% endblock %}

{% block body %}
<h1>Card</h1>

    <p>class: DeckOfcards<br>
            DeckJoker<br>
            Card<br>
            I klassen DeckOfCard har jag en array med kortlekn och även en komposition av Card klassen.
            DeckJoker klassen ärver från DeckOfCards klassen och lägger till tre stycken jokrar.</p>

    <a href="{{ asset('img/uml_card.png') }}">
        <img src="{{ asset('img/uml_card.png') }}" alt="">
    </a>

<ul>
    {# <li><a href="{{ path('card') }}">Landing page</li> #}
    <li><a href="{{ path('deck') }}">Deck</li>
    <li><a href="{{ path('joker') }}">Joker</li>
    <li><a href="{{ path('shuffle') }}">Shuffle</li>
    <li><a href="{{ path('draw') }}">Draw one card</li>
    <li><a href="{{ path('draw_many', {num: 2}) }}">Draw many cards</li>
    <li><a href="{{ path('reset') }}">Reset</li>
</ul>

{% endblock %}
