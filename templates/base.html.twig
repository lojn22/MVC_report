<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/glider.svg') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="site-header" style="background-image: url({{ asset('img/almond.jpg') }})">
            <span class="site-title">My Symfony Site</span>
        </header>

        <nav class="navbar">
            <ul>
                <li><a href="{{ path('start') }}">Start</a></li>
                <li><a href="{{ path('about') }}">About</a></li>
                <li><a href="{{ path('report') }}">Report</a></li>
                <li><a href="{{ path('lucky') }}">Lucky</a></li>
                <li><a href="{{ path('card') }}">Card</a></li>
                <li><a href="{{ path('api') }}">API</a></li>
                <li><a href="{{ path('game')}}">21</a></li>
        </nav>

        <main>{% block body %}{% endblock %}</main>

        <footer>{% block footer %}©Louise{% endblock %}</footer>
    </body>
</html>
